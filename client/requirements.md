## Packages
framer-motion | Essential for page transitions, smooth galaxy hover effects, and premium animations.

## Notes
- Using the pre-installed Google Fonts `Oxanium` (for display/headings) and `Outfit` (for body text).
- Using Shadcn UI components for structure (Cards, Accordion, Avatar, Skeleton, Button).
- Dark/Galaxy theme is forced by default via CSS variables and styling.
